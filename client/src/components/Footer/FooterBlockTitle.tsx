const FooterBlockTitle = ({ children }: { children: string }) => {
  return (
    <>
      <div className="text-[#fff] text-[16px] font-[600]">{children}</div>
    </>
  );
};

export default FooterBlockTitle;
